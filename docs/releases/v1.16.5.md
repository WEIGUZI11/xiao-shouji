# 小手机 v39 / 1.16.5：浏览器返回与搜索修复

本次修复内置浏览器进入后无法返回的问题，并整理相关交互。

## 本次更新

- 左侧返回可逐级返回文章、搜索页和首页，不再反复选回第一条搜索记录；右上角可直接返回小手机桌面。
- 阅读文章时，切换书签、历史和设置会正确显示目标页面。
- 搜索进行中阻止重复提交，避免连续回车或刷新产生额外的本地回退记录。
- 检查模型结果中的文字和网址，兼容 JSON 代码块；无有效结果时使用原有本地回退，并明确提示。
- 收藏、搜索和世界书导入提示在各页面均可见。
- 修复首页与文章、书签、历史或设置同时显示的问题。

## 安装与数据

- 版本：1.16.5；versionCode：39；包名：`com.smallphone.app`。
- 请保留导出备份，并在原应用上直接更新安装，不要先卸载或清除数据。
- 保持历史签名和存储地址 `https://small-phone.local/`，不清空聊天、搜索记录、书签和其他玩家数据。
- 如安装提示签名冲突，请停止安装并反馈，不要卸载旧版来解决。

## 验证范围与已知限制

- 全套 99 个本地测试通过；类型检查、网页构建和 Android release 构建通过。
- APK 签名验证通过，已与 GitHub v38 / 1.16.4 的旧包核对包名、签名和递增版本号；安装包包含与本次生成网页完全一致的内容、最新修复及 10 个字体资源。
- 本地回退仍是虚拟搜索内容，不代表真实联网搜索结果。
- 未进行 Android 真机触控、真实模型接口和玩家实际数据升级验收；本地验证不等同于真机验收。

## 下载校验

APK：`small-phone-v39-1.16.5.apk`（97,807,924 字节）

SHA-256：`68304885B5CB8F70132AFC099F6FCBCF18AE19A3D72BD644A8F101A636797086`
